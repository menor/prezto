#
# Executes commands at the start of an interactive session.
#
# Authors:
#   Sorin Ionescu <sorin.ionescu@gmail.com>
#

# Source Prezto.
if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
  source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"
fi

# Source iterm shell integration
source ~/.iterm2_shell_integration.`basename $SHELL`

# Run nvm
# export NVM_DIR=~/.nvm
# source $(brew --prefix nvm)/nvm.sh

# Alias for running local npm modules
# alias npm-local='PATH=$(npm bin):$PATH'

export TERM=xterm-256color-italic

# Applications alias
alias telegram='/Users/menor/code/tg/bin/telegram-cli -k tg-server.pub'
wt() { curl http://wttr.in/$1; }

export PATH="$PATH:$HOME/.rvm/bin" # Add RVM to PATH for scripting
export PATH="$PATH:`yarn global bin`" # yarn global packages

test -e "${HOME}/.iterm2_shell_integration.zsh" && source "${HOME}/.iterm2_shell_integration.zsh"
